# Maintainer: Anthony Davies <anthony.t.davies@gmail.com>
pkgname=external-attacher
pkgver=2.1.0
pkgrel=0
pkgdesc="Sidecar container that watches Kubernetes VolumeAttachment objects"
url="https://github.com/kubernetes-csi/external-attacher/"
arch="x86_64 aarch64 armv7"
license="Apache-2.0"
options="!check"
makedepends="go bash" 
source="$pkgname-$pkgver.tar.gz::https://github.com/kubernetes-csi/$pkgname/archive/v$pkgver.tar.gz"
builddir="$srcdir/src/github.com/kubernetes-csi/$pkgname"
subpackages="csi-attacher"
export GOPATH="$srcdir"


prepare() {
	mkdir -p ${builddir%/*}
	mv "$srcdir"/$pkgname-$pkgver "$builddir"/ || return 1
	cd "$builddir"
	default_prepare
}


build() {
	make
}

package() {
	echo Main package
	mkdir -p $pkgdir
	chmod -R u+w $srcdir || return 0
}

attacher() {
	echo Packaging csi-attacher
	cd $builddir
	mkdir -p $subpkgdir/usr/bin
	cp bin/csi-attacher $subpkgdir/usr/bin/
}
clean() {
	chmod -R u+w $srcdir || return 0
	rm -rf $srcdir
}

sha512sums="ee0af771eb18e64e5f7abada2f14a86491caea6e5083202daa268ae105de0b9367c2f044433f555c82f39b711ca82124f0a1c89648a5d9b72c2f0ace92b42b09  external-attacher-2.1.0.tar.gz"
