---
kind: pipeline
name: edge-armv7

platform:
  os: linux
  arch: arm

clone:
  disable: true

steps:
- name: build
  pull: always
  image: phoenixmage/alpine-drone-ci:edge-armv7
  commands:
  - free
  - cat /sys/fs/cgroup/memory/memory.limit_in_bytes
    #  - build.sh

trigger:
  event:
    - pull_request
...
